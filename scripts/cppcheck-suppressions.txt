# Suppress diagnostics emitted from Arduino/ESP-IDF dependencies that are
# transitively included by the firmware. Code under shotStopper/, libraries/,
# and idf/main remains in scope.
*:**/Arduino15/**
*:**/Arduino/libraries/**
*:**/esp/esp-idf/**
*:**/idf/managed_components/**
*:**/idf/third_party/**
# FreeRTOS semaphore macros expand to C casts; not actionable at call sites.
dangerousTypeCast:*/shotStopper/ShotStopperNetwork.cpp
dangerousTypeCast:*/shotStopper/ShotStopperWebhook.cpp
dangerousTypeCast:*/shotStopper/ShotStopperTaskMutex.h
# This deliberately uninitialized RTC record preserves reset evidence across a
# reboot. It is always validated by magic, inverse fields, and checksum before
# its contents are consumed; initializing it would defeat that safety guard.
uninitMemberVarNoCtor:*/shotStopper/ShotStopperResetGuard.h
# unusedFunction false positives are suppressed inline at the call-less
# Arduino framework entry points (setup/loop in shotStopper.cpp).
