#!/usr/bin/env bash
# OTA is intentionally disabled; firmware installation is USB-only.
set -euo pipefail

echo 'OTA is disabled; install firmware over USB with ./scripts/dev flash --confirm.' >&2
exit 2
