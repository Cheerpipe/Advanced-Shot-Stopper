# Manual Test Plan

These are acceptance procedures, not instructions to operate hardware
automatically. Start on an isolated bench. Connect a live machine only after
the applicable electrical checks pass.

Memory-placement changes require M39/M81/M90 evidence on both PSRAM targets.
Exercise profiler start/stop, malformed and concurrent JSON requests, repeated
settings writes and webhook cancellation while flash operations are active.
Confirm no cache-disabled access panic, corruption, lost persistence completion
or unexplained allocation-failure delta; record internal/PSRAM free, minimum
and largest blocks before and after warm-up. Capture all task stack margins
in bytes, using short profiler runs for tasks absent from continuous health.
Allocation-failure injection in host tests does not replace this target gate.

## Select and record a run

Use [VALIDATION.md](../VALIDATION.md) to select the required gate. Record
firmware/git version, board revision and PSRAM architecture, compile flags,
machine/switch type, scale model/firmware, settings, measured timings and
pass/fail evidence. A missing prerequisite is not a pass.

Unless a row says otherwise, workflow examples assume Natural paddle mode,
Quick rinse off, and a usable scale. Adapt only through the documented
[machine-type contract](settings/momentary.md#stopping-and-time-limits);
relay-open and group-stopped are not interchangeable on momentary machines.

Existing IDs are retained; the formerly duplicated USB M66 is now **M66-USB**,
while drip-delay M66 keeps its ID. Current release requirements are defined by
[scheduling](SCHEDULABILITY.md), [resource budgets](P2_RESOURCE_BUDGETS.md),
and [target traces](P2_TARGET_TRACE.md), not by a historical pass.

## Sections

- [Boot, electrical safety and reset](#boot-electrical-safety-and-reset)
- [Brewing, cup, scale and presets](#brewing-cup-scale-and-presets)
- [Network, Web UI and access](#network-web-ui-and-access)
- [Recovery](#recovery)
- [OTA and runtime qualification](#ota-and-runtime-qualification)
- [Momentary behavior and scale discovery](#momentary-behavior-and-scale-discovery)
- [Historical cutover evidence](#historical-cutover-evidence)

## Boot, electrical safety and reset

| ID | Procedure | Expected result |
| --- | --- | --- |
| M01 | Power the controller with the paddle OFF. | Relay is open at boot; after debounce, state is `READY`. |
| M02 | Power it with paddle ON, then release it. | Machine circuit stays open until a stable OFF is detected. |
| M18 | Reset or remove controller power while measuring relay COM/NO. | Contact remains open; there is no unintended close pulse. |
| M20 | Open the Web UI status with the external safety macros disabled. | `Circuit safety` reports timers and WDT ready, plus `external not configured`; this build is identified as software-only protection. |
| M21 | On an isolated bench load, use an instrumented test build to stop `loopTask` immediately after machine circuit closes. Do not attach OpenOCD during the measurement. | GPTimer/K2 opens the machine circuit within the configured measured limit and TWDT reboots the board within its 5 s timeout; the boot never recloses the machine circuit. |
| M22 | Freeze only `scale_worker` while leaving control responsive. | Its own TWDT subscription causes a reboot within 5 s; machine circuit opens on or before reset and does not resume afterward. |
| M23 | Freeze only `network_manager` while leaving control responsive. | Its own TWDT subscription causes a reboot within 5 s; no network task can directly restart or close the machine circuit. |
| M24 | Build with K2/feedback enabled, disconnect the isolated feedback input, and request a close. | Feedback fails to confirm within 100 ms; K1 opens and safety enters `LOCKOUT`. |
| M25 | Force feedback to the CLOSED level before boot and before a close request. | The supervisor reports `FEEDBACK_STUCK_CLOSED`, remains in `LOCKOUT`, and never energizes K1. |
| M26 | While K2 is energized, hold heartbeat permanently LOW, then permanently HIGH, and finally disconnect it. | The external detector opens K2 within its independently specified timeout in all three cases. |
| M27 | Simulate a welded K1 or shorted K1 driver using the isolated bench fixture, then remove heartbeat. | K2 still opens the series machine circuit path within its measured non-retriggerable limit. |
| M28 | Repeat M01, M02, M07, M18 and M21-M27 on ESP32-S3 N8R4 and N16R8. | Every target passes with measured timings recorded; no idle-task starvation or false WDT resets. |
| M29 | Run brownout, power-cut, Wi-Fi/RF stress and a 72 h soak followed by 10,000 relay cycles on the isolated fixture. | No spontaneous close, over-limit close, resumed cycle after reset or undiagnosed feedback mismatch occurs. |
| M37 | Factory-reset two devices, verify factory access, then change each password independently. Record only success/failure, never the secrets. | Both initially use the documented factory password. Each replacement works only on its own device; Admin/OTA/AP agree on that device's password. Replies do not echo secrets. |
| M38 | Force CPU-frequency setup, EEPROM initialization and BLE initialization failures one at a time on an instrumented build. | Frequency/safety initialization failure prevents machine circuit close; EEPROM failure uses volatile safe defaults without network persistence; BLE failure permits only local manual operation. |
| M39 | During a 72 h target-specific soak, record `/api/v1/status/admin` health fields and firmware size on every build. | Loop/task age and stack remain bounded, heap minimum/largest block do not trend toward exhaustion, drops are explained, and every `.bin` stays below the CI budget. |
| M40 | Boot with the onboard GPIO1 LED wired as documented and **Blue LED while scale connected** enabled. | The LED stays LOW through boot with no scale; connecting a scale drives GPIO1 HIGH without relay movement; disconnecting returns it LOW. |
| M43b | Trigger a watchdog panic/reboot while machine circuit is closed, keep the paddle OFF after reset, and open every WebUI view. | The panic callback opens the machine circuit before reboot and `setup()` holds it OPEN; the previous cycle does not resume. The controller reaches normal Ready without a recovery gesture or unsafe override; WebUI configuration, rinse, and the next machine circuit cycle work normally. |
| M44 | Short or disconnect the GPIO1 LED, then repeat a normal paddle stop and the 60-second hard-limit test. | LED wiring failure cannot delay the control loop, keep machine circuit closed, or affect either hard deadline. |

## Brewing, cup, scale and presets

| ID | Procedure | Expected result |
| --- | --- | --- |
| M03 | With **Quick rinse** ON, from Ready turn paddle ON for less than the rinse gesture threshold, then OFF. | Rinse starts, machine circuit remains closed for configured rinse duration, then opens. With Quick rinse OFF, the same short ON→OFF is a shot, not `RINSE`. |
| M04 | Hold paddle ON past the rinse gesture with a connected scale. | State becomes `BREW` quickly (after debounce/BLE); first drops beep when coffee starts (independent of the retare window). |
| M05 | Repeat M04 with beep disabled. | Brew starts normally and no first-drops beep command is sent. |
| M06 | With Natural paddle mode and Quick rinse off, turn paddle OFF during brew/manual. | Machine circuit opens immediately; next cycle requires normal rearm. |
| M07 | On Natural paddle firmware, leave an automatic BBW cycle ON until the configured wall time. | Machine circuit opens no later than the configured limit, never later than 60 s. |
| M08 | Disconnect the scale during brew, then reconnect it and resume valid notifications. | Weight control becomes `SUSPENDED` without silently changing the workflow to manual, then returns to `ACTIVE` only after three coherent samples from the new connection generation. Verify the selected mode's physical stop and applicable hard limits; Auto/Original OFF is not a universal immediate stop. |
| M10 | Change a workflow field during brew, rinse, and manual states. | UI is disabled and API rejects each change; current snapshot is unchanged. |
| M11 | Save valid workflow settings while Ready, restart, and reopen UI. | Values persist and apply to the next cycle only. |
| M12 | Enter a relationship-invalid timing configuration. | Server rejects the complete transaction; no field changes. |
| M19 | From Ready, set **Baseline offset**, save, press **Reset learned stop offset to baseline**, confirm, then restart. | The learned offset is reset to the configured baseline (factory default 1.5 g) and remains there; a pending post-shot analysis cannot overwrite it. |
| M33 | Disconnect or freeze the scale before its first weight, then repeat after allowing the last weight to become older than 1 s. | A cycle without initial scale authority remains manual; an automatic cycle with a stale stream publishes `STALE`/`SUSPENDED` and never uses stale weight. |
| M34 | During an automatic shot, inject NaN, one isolated abrupt sample, and then two fresh abrupt samples above 1,000 g as if pressing the scale with a finger. | Invalid/outlier data never enters regression or learning. One sample suspends control; a confirmed overload opens the machine circuit with `WEIGHT_ANOMALY`, even before ten regression samples exist. |
| M35 | Force the scale-command and event queues full while ending a shot. | Machine circuit opens immediately; the control loop never blocks. The scale STOP is retried within its bounded window and drop counters become visible. |
| M41 | Disable **Blue LED while scale connected**, connect a scale, then re-enable the setting. | GPIO1 stays LOW while the setting is off even if the scale is connected, and goes HIGH when the setting is saved on. Machine circuit behavior remains controlled by the state machine. |
| M45 | Start an automatic shot with the cup already on the scale before paddle ON. | Initial tare only; retare window runs but does not retare a stable full cup; first drops beep when coffee starts (independent of the retare window); shot stops at target weight after BBW protection ends. |
| M46 | Power-cycle the scale, reconnect, place a cup, and run the first paddle shot. | The first shot enters automatic brew; the log does not show `MANUAL_NO_SCALE` from missing fresh weight. |
| M47 | During early brew with rejections, inspect the Web UI log. | Each rejection names the reason (`slew`, `range`, etc.) and includes numeric weight context. |
| M48 | Run an automatic brew to target weight after M45 or M46. | Machine circuit opens at the BBW target after BBW protection ends (`normal_target`). |
| M49 | Start an automatic shot with the cup off the scale, then place a ~150 g cup within 1 s of brew start (a real put-down may overshoot, e.g. 160→150). Repeat with the cup at ~3 s (still inside the default 4 s retare window). | Automatic retare occurs once; **no first-drop beep** when the cup lands; shot timer does not restart; first drops beep once when coffee starts (against tare zero, even during **Post-tare grace**); shot stops at target weight after grace settle. |
| M50 | Repeat M49 with automatic retare disabled. | No retare window; BBW protection still runs from shot start. |
| M51 | During the retare window, place only a stable ~7 g object (below minimum cup weight in **Settings → Machine and scale → Cup**). | No retare; retare window expires; BBW protection continues until timeout. |
| M52 | Place a heavy cup (> target weight) during the retare window. | Shot does not stop by weight until BBW protection ends. |
| M53 | Let BBW protection timeout expire without detecting drops, then allow coffee to accumulate. | No beep at timeout; when first drops are later detected, optional beep plays once; weight stop then works normally. |
| M54 | Shorten BBW protection below retare window + 3 s in the Web UI and save. | Server rejects the transaction or clamps to the minimum; UI hint explains the rule. |
| M55 | With automatic retare off, run an automatic shot. | Retare window is skipped; BBW protection still inhibits weight stop until it ends. |
| M58 | Sign in → Settings → Brew: confirm Single/Double cards; Load Single then Double; form goal/min/rec/baseline follow the active preset without copying into a separate machine brew blob. | Active card highlights; composed status matches the preset; reboot keeps the same active id. |
| M59 | Edit Double goal, Save preset, pull a shot that learns offset; switch to Single then back to Double. | Learned offset stays on Double; Single keeps 0.5 g baseline/offset; Reset learned offset restores that preset’s baseline. |
| M60 | New → Untitled (FW Double seed 36 g / baseline 1.5); Duplicate Double → “Double copy”; Duplicate again → “Double copy 2”; rename copy inline (desktop) or dialog (phone); Delete a custom until one remains (last delete rejected). | Names and seeds match; rename persists without rewriting learned fields; cannot delete the last preset. |
| M66 | In **Settings → Machine and scale → Scales**, set **Drip delay** to 0 s, run a shot, then repeat with 3.0 s and 10.0 s while watching Last Shot/history and the scale weight after machine circuit opens. Reboot after saving and verify the selected value remains. | `status/settings.config.dripDelayMs` reports 0, 3000, and 10000 respectively. Final weight/history update on the next control loop at 0 s and only after the configured window at 3/10 s. The saved value survives reboot; starting another shot during a pending window commits the previous shot with its last-known weight. |
| M66B | With a local buzzer build, select **Buzzer only**, enable **Bullseye melody**, paste a valid RTTTL tune (≤500 characters), and save. Finish automatic, timer-only, and manual shots at the exact target; also try a brief target touch, a stable non-target weight, Scale priority, Sound alerts OFF, and disabling/re-enabling Bullseye. | The custom tune plays once after 1 continuous second of fresh exact-target samples, including before drip delay expires. It stays silent for brief/non-target runs and outside Buzzer only. Disabling makes the textarea gray/read-only without erasing it; re-enabling and rebooting restore the saved tune. Rinses never trigger it. |

## Idle tare and tared cup acceptance

Run these hardware checks only with explicit authorization. Record scale model,
firmware, machine type, build, and observed result; host tests are not hardware
qualification.

| ID | Action | Expected result |
| --- | --- | --- |
| IT-M01 | Observe an empty pan, then place a cup with idle tare ON. Leave it at zero; add a spoon; remove and replace it. | One tare per confirmed placement, no scale timer start, no repeated tare while PRESENT. |
| IT-M02 | Enable Require cup to start. Idle-tare the cup, then start at zero; repeat with shot-start tare OFF. | Cup guard accepts known PRESENT at zero; all other protections remain active. Removal during the shot still stops when configured. |
| IT-M03 | Finish a shot and leave the full cup on the pan until weight settles. Then remove/replace it while the paddle remains ON after stop. | No tare merely at stop/settling. New placement tares the full cup, without clearing REQUIRES_OFF or reactivating the machine. |
| IT-M04 | Replace the cup during drip delay. | Prior last shot/history/end event use captured last-known weight; replacement weight/zero does not enter learning. |
| IT-M05 | Start immediately during queued/executing idle tare; also disconnect during the request. | Start gesture is refused until pending work resolves; release and retry. No stale tare enters a new shot/connection. STOP remains responsive. |
| IT-M06 | Reconnect with a pre-tared cup at zero; try the scale's physical tare button separately. | No fabricated placement on reconnect. Remove/replace for detection. Physical-button tare recognition remains unsupported without verifiable protocol evidence. |
| IT-M07 | Disable idle tare, save, reboot; then factory reset or upgrade a legacy configuration. | Saved OFF survives reboot; factory reset and first migration initialize ON. Presets do not change the switch. |

## Network, Web UI and access

| ID | Procedure | Expected result |
| --- | --- | --- |
| M09 | On a deliberately opt-in build with `SHOT_STOPPER_ENABLE_REMOTE_MACHINE_CONTROL=1`, unlock **Admin**, open Home, and use **Start shot** / **Stop shot**. On momentary-only, stop once after about 3 s while Machine still shows Assumed on. Then use the smaller **Force press** action from Ready and during a cycle; repeat on momentary+reed and confirm it is absent on paddle. | Without unlock, Home shows the version footer and no Actions panel; APIs return `ADMIN_LOCKED`. After unlock, Start and Stop each produce a distinct configured switch pulse, including the early Assumed-on stop. Force press emits one pulse without changing the logical cycle/state and remains subject to relay safety. It is shown only for momentary builds. On the default build all close-producing controls stay disabled and are rejected by the API. |
| M13 | Boot without STA credentials; leave SoftAP idle for more than 3 minutes. | SoftAP stops; `http://192.168.4.1/` is unreachable until USB `AP_START` or reboot. |
| M14 | Save valid STA credentials (DHCP) and restart. | Full-screen wait overlay counts down from 180 s and hides Reload; SoftAP stops after STA associates; Serial prints DHCP address; rejoin the home network and open the DHCP address to claim the UI and confirm the pending config. |
| M14b | Save valid static IP and restart; stay on the wait overlay or open the new IP within 3 min. | STA uses configured IP; status shows static mode; config becomes CONFIRMED. Overlay reconnects if the address is unchanged. |
| M14c | Save unreachable/wrong static IP (or leave the wait overlay until 0 s). | Overlay keeps polling after the countdown hits 0. Pending config reverts to last-known-good (or clears STA); overlay reconnects on the previous address when it returns. STA retries continue when credentials remain. SoftAP auto-raise only if STA never joined this boot; otherwise recover with USB `AP_START` or reboot. |
| M15 | Save invalid/unreachable STA credentials and restart. | After the ~25 s attempt timeout, SoftAP is available and STA association retries continue. SoftAP idle-stops after 3 min with no SoftAP stations; STA retries continue without SoftAP. |
| M15b | With confirmed STA online, disable the home AP / force a link drop for less than ~25 s, then restore it. | SoftAP does not appear; STA recovers and Web UI stays on the STA IP. |
| M15c | With confirmed STA online, disable the home AP for more than ~25 s. | SoftAP does **not** appear; STA association retries continue. SoftAP recovery requires USB `AP_START` or a reboot (boot SoftAP path). |
| M16 | Scan Wi-Fi in Ready, then start a physical cycle. | Scan is cancelled; control and relay behavior are unaffected. |
| M17 | On the remote-control opt-in build, leave a browser virtual paddle ON, then close/disconnect the browser. | Machine circuit stays under shot limits / physical paddle / Stop; closing the browser alone does not open the machine circuit. |
| M30 | With the default build, authenticate from two browsers and inspect the Actions panel and API. | Without Admin unlock, Home shows the version footer and no Actions panel. Start shot, rinse, and STOP all return `ADMIN_LOCKED`. |
| M31 | With remote control deliberately enabled, unlock Admin on browser A and start a Web cycle. From browser B (no unlock), attempt paddle/rinse/stop; then tap **Lock** in the header or Admin on A. | Browser B cannot start, rinse, or STOP (`ADMIN_LOCKED`). **Lock** on A clears Admin unlock and hides Actions; further Start/Stop/rinse on A return `ADMIN_LOCKED`. The WebUI claim stays; the paddle still opens the machine circuit. |
| M31b | Unlock Admin, open Home, and leave it polling with no Start/Stop/rinse and without returning to Admin. Then unlock again, stay on Admin, and separately tap **Lock**. | After 15 min on Home only, Actions disappear and header **Lock** hides (`adminUnlocked` false). Staying on Admin or using Start/Stop/rinse renews the 15 min. **Lock** hides Actions immediately. |
| M32 | While Ready, request each configuration, Wi-Fi, scan and factory operation, then move the physical paddle before its maintenance settle time. | The operation is canceled, machine circuit stays open, state requires physical OFF, and no partial runtime setting is applied. |
| M36 | Fail AP creation and HTTP startup independently, then remove each fault without rebooting. | Partial startup rolls back and retries with bounded backoff until the AP/UI recovers; control remains responsive and fail-open throughout. |
| M56 | Open the Web UI home, then reload and claim while a scale is connected. | First HTML load is gzip (`Content-Encoding: gzip`). JS loads from `/app.js?v=…` and CSS from `/app.css?v=…` (both gzip). Reload and post-claim reload return **304** for HTML when the firmware is unchanged; JS/CSS are cached via versioned immutable URLs. Scale stays connected; no `PACKET_TIMEOUT`. Safari does not show “Device unreachable”. |
| M57 | On home, open DevTools Network: stay on `/` for ~10 s, then open `/log`, `/stats`, `/debug`, `/admin`, and `/settings`. Scroll the shot log table if more than 10 shots exist. | Home only polls `/api/v1/status/home`. Log polls `/api/v1/log` only while on `/log`. Stats polls `/api/v1/shots?offset=0&limit=10` only while on `/stats`; scrolling requests later pages of 10. Admin/Debug/Settings poll `/api/v1/status/{admin|debug|settings}` only while on those views. Deep-link refresh of `/log` serves the SPA and shows the log view. Deep-link `/debug` while signed in shows Debug. |
| M61 | Home: turn Brew by weight OFF (Manual), pull a timer-only cycle, turn ON again; open Settings brew checkbox. | Manual is session override and does not persist `brewByWeight=false` into the recipe; Save preset while Manual was on still keeps recipe BBW if the Settings Brew checkbox stayed on. |
| M62 | Exercise New / Duplicate / Save / Delete / Load while watching Serial for `network_manager` stack canary; repeat with Wi‑Fi clients polling status. | No reboot/canary; heap stays healthy; status `presets` JSON remains bounded. |
| M63 | On a build with `SHOT_STOPPER_ENABLE_BUZZER=1` and a passive piezo on the configured GPIO, from Ready open Debug and play short, long, double, triple, **Slow (3 s)**, **Medium (3 s)**, **Fast (3 s)**, **Rapid (3 s)**, **Chime**, **Swing**, **Echo**, **Echo inverted**, **Morse**, and **Snap**; set Alerts **Output channel** to Buzzer only, confirm **Scale connected** and **Scale lost** are visible and ON, **Extended shot pulse** and **Slow extended pulse** to Fast; trigger tare/start/first-drop/paddle-off, a scale disconnect, a scale connect/reconnect, a Fast extended shot (target before min BBW brew time) so the Fast pulse runs until cut, and optionally a Slow extend (past max BBW brew time) for the Slow pulse; then set Scale priority and confirm Scale connected / Scale lost still play on the local buzzer, while paddle reminder prefers the scale when connected. | Debug buttons play 2700 Hz PWM motifs (Slow/Medium/Fast/Rapid = looping 20 ms ticks at 2/3/4/5 per second for 3 s). Live alerts are RTTTL; extended-shot pulses use B4 at the selected rate until cut. Buzzer only routes alert sound to the local buzzer; Scale priority uses the scale when connected except scale-incapable cues (Scale lost/connected, ATM / no-scale, extended pulses). Machine circuit stays open during debug. |
| M64 | With **Warn once, then allow** (default), BBW ON, and the scale off: from Ready, paddle ON **past the rinse gesture**, then OFF; confirm Status **No-scale guard** goes Armed → Idle and machine circuit stays open (local buzzer cue on paddle ON if buzzer compiled and *Manual without scale* is ON). Shot **No-scale guard** stays at the last-shot value (or — if none). Paddle ON again. With **Quick rinse** ON, also try a **short** rinse gesture while Armed (machine circuit must close, guard goes Idle, same local cue on paddle ON). Then reconnect the scale, or wait the configured **Protection returns after**. | Long first start stays `READY`, machine circuit open, Status Idle; live no-scale RTTTL (three G4 ticks) on paddle ON, not after the rinse window. Second start is `MANUAL_NO_SCALE` and machine circuit closes (same cue again). Short Armed gesture with Quick rinse is `RINSE` and goes Idle (cue on paddle ON). That live cue is not Debug Triple (80/70 ms). Serial debug is **off** by default (Debug checkbox / `SERIAL_DEBUG_ON` at **115200** baud). Scale reconnect or cooldown returns Status Armed. Web rinse also consumes the guard when Quick rinse is on. Idle rinse/brew are unchanged. BBW OFF or the setting OFF does not block. |
| M64b | Select **Require a scale**, leave BBW ON and the scale unusable, then attempt two long starts and a quick-rinse gesture. From OFF, cycle the activator ON→OFF three times within two seconds; wait for the scale-connected melody, then make a fresh start. Repeat too slowly, then hold the activator while reconnecting the scale. Finally turn BBW OFF. | Ordinary no-scale attempts stay blocked. The fast three-cycle gesture leaves the machine idle, plays the confirmation melody, and allows later manual shots/rinses until the configured cooldown expires. A slow gesture stays blocked. Reconnection while held does not start anything; a new gesture with fresh weight works. BBW OFF restores manual use. |
| M65 | Home Quick Settings: with Brew by weight ON, toggle **No-scale BBW**, **Fast extraction guard**, **Slow extraction guard**, **A→M time guard**, and **Cup protection**. Open Settings: the matching controls follow. In **Settings → Alerts**, turn **Sound alerts** OFF and trigger first-drop, paddle, completion, scale-connect/lost and extended-pulse events; then turn Brew by weight OFF. | Each switched Home control turns yellow and read-only until status confirms (or up to ~5 s, then snaps back). Sound alerts is Settings-only (Save machine settings), silences both output routes, and makes every other Alerts control read-only without changing its stored value. The BBW-dependent Home switches remain read-only with BBW OFF and re-enable with BBW ON. |
| M66-USB | USB serial at **115200**: `HELP`, `DEBUG_STATUS`, `DEBUG_FULL`, `WIFI_STATUS` / `AP_STATUS` / `WEBUI_STATUS` / `NET_STATUS`, `HEALTH`, `SCALE_STATUS`, `NTP_STATUS`, `LOG_DUMP`. Then `WIFI_CONNECT` / `WEBUI_RESTART` and, with STA up, `AP_START` (SoftAP must stay up). Watch the action log. Finish with `DEBUG_OFF`. See [USB serial CLI](SERIAL_CLI.md). | `HELP` lists the new verbs. Status dumps print without a Ready gate. `DEBUG_FULL` persists serial+ring at debug; connect/start/restart print detailed OK/ERR lines even before debug is on. Automatic STA retry / SoftAP raise stay quiet until debug is on. `AP_START` while STA is connected leaves SoftAP up (`mode=AP_STA`). `DEBUG_OFF` silences traces after the OK line. |
| M67 | Open the Web UI, then leave it without using a control for 15 min while watching DevTools Network. Scroll and switch browser tabs during the wait. | At 15 min the page stays open, all controls lock, polling stops, and a full-screen overlay with the brand mark and **Reload** fades in. Scrolling and changing browser tabs do not reset the timer. Select **Reload** and confirm the overlay fades out and polling resumes for the visible view. |
| M68 | With a scale connected, open ShotStopper Companion and scan for the stopper. Exercise FF10–FF20, write SSID then password through FF22/FF23, observe FF24, write FF21, reboot through FF25, and toggle AP through FF26. Set `BLE_COMPAT_DISABLE`, verify status before reboot, reboot, inspect `HEALTH`, then re-enable and reboot again. | The app discovers `shotStopper` by service `0FFE`; the scale remains connected. Valid Ready-state settings persist, invalid/active-shot writes revert, FF21 stays false and never starts OTA, IP/AP notifications follow effective state, and FF25 reboots only from Ready. Before reboot, status reports configured disabled, active this boot, and restart required. After reboot there is no Companion advertising/client/profile allocation, BLE scale support remains functional, and heap increases. Re-enabling likewise remains pending until reboot. |

## Recovery

| ID | Procedure | Expected result |
| --- | --- | --- |
| M69 | On an isolated bench, follow the three-cycle procedure in [EMERGENCY_RECOVERY.md](EMERGENCY_RECOVERY.md), including the 3 s confirmation. | Machine circuit remains open; three 50/50 ms pulses sound; the controller restarts with STA/LKG/IP cleared and device password `ineedacoffee`, while presets, calibration and history remain intact. |
| M70 | On an isolated bench, follow the five-cycle procedure in [EMERGENCY_RECOVERY.md](EMERGENCY_RECOVERY.md). Repeat with four cycles, a gesture longer than 5 s, movement during confirmation, and expiry of the 60 s window. | Only five valid cycles produce five 50/50 ms pulses and a full factory reset. Invalid attempts erase nothing; timeout gives the 1.5 s exit beep; machine circuit never closes. |
| M71 | Start each recovery operation and cut power between intent write, individual store resets, verification and restart. Repeat on builds with passive and disabled buzzer. | The next boot resumes the pending idempotent reset before Wi-Fi/BLE; success data matches the documented scope. Without buzzer behavior is otherwise identical. A forced NVS failure produces the distinct error motif, **drops the recovery latch**, and **continues startup** (SoftAP) instead of hanging with machine circuit open. |
| M-AUD-07 | Factory reset: cut power after the first factory settings slot write and before the second (no namespace `clear()`). Repeat after settings factory and before BLE companion reset. | Next boot still loads a valid settings blob (previous or factory). Device password/Wi-Fi are not wiped to an empty namespace. If recovery intent is pending, M71 resume still completes the remaining stores. Machine circuit stays open. |

## OTA and runtime qualification

| ID | Procedure | Expected result |
| --- | --- | --- |
| M72 | After an Arduino-ESP32 core bump (pinned **3.3.11**), on a bench S3+PSRAM board with machine circuit disconnected from the machine: boot, confirm Serial 115200 has no `ble=fail`, connect a scale with fresh weight, open the Web UI (claim, home polling, one Settings save), capture USB `HEALTH` at boot and again with UI+scale, keep AP or STA up during BLE scan, run `WEBUI_RESTART` with the scale connected. If Companion is in the build, discover service `0FFE` while the scale stays connected (reduced M68). | The NimBLE host reaches ready state; scale stream stays fresh with the browser open; Web UI loads and polls; internal `heapFree`/`heapLargest` are not exhausted and PSRAM is in use; no `health heap low`; HTTP returns after `WEBUI_RESTART` without dropping the scale. Abort and revert the core pin if the host fails to initialize, the Web UI will not start, internal heap has no usable large block, the controller resets spontaneously, or the scale dies when the browser opens. This is a ~15–30 min smoke gate, not M29/M39. |
| M73 | Same bench setup as M72: leave the scale connected and a browser on home (polling) for 2–4 h. Every 30–60 min capture USB `HEALTH` and `/api/v1/status/admin`. | Internal `heapMin` does not stair-step toward exhaustion; no burst of `PACKET_TIMEOUT`; no WDT/panic; UI keeps responding. A 72 h M29/M39 soak and shots on-machine are optional before calling the core production-validated and do not block the pin if M72 and this short soak pass. |
| M74 | On an isolated bench with machine circuit disconnected, with the device password still at the factory default (or any set password), run `./scripts/bo-idf --arch <board> --host <ip>` (use the hidden password prompt) with a build whose `VERSION` was bumped. Watch the OTA progress, the identity the controller reports back, and the restart. | The OTA is accepted, the reported identity matches the local `./scripts/image_tag.js` output, and the controller restarts into the new version. `/api/v1/status/admin` shows the new `running` version and `confirmed: true` within ~20 s. Wi-Fi credentials, presets, calibration and shot history are unchanged. Machine circuit never closes at any point. |
| M75 | Repeat M74 from **Admin** (unlock with the device password) **→ Firmware update** in the Web UI, in two steps. | *Upload and verify* shows a progress bar and then the verified version and board; *Flash and restart* stays disabled until that verification succeeds. **Discard** returns the panel to "No verified image." without touching the running firmware. |
| M76 | Attempt an update with: a wrong device password; an image built for the other board; an image older than the running one; a random non-firmware file; and a truncated `.bin`. | Each attempt is refused with a named reason (`DEVICE_PASSWORD_INVALID`, `ARCH_MISMATCH`, `DOWNGRADE`, `BAD_IMAGE`, `VERIFY_FAILED`) and the controller keeps running its firmware. `/api/v1/status/admin` still reports the original `running` version. |
| M77 | Interrupt a resumable upload over Wi-Fi, then retry the same image. Repeat with power loss around a durable checkpoint. Separately reboot after staging. | The running slot remains untouched. Matching partial sessions resume from the validated offset, which can retreat to the last 512 KiB checkpoint after reboot. Staged state is RAM-only: reboot after staging needs a new upload. A different image needs explicit discard. |
| M78 | Start an upload while Ready, then move the physical paddle (or close the machine circuit) mid-transfer. | The update is cancelled with `SAFETY_LOST`; the machine's cycle behaves normally and is never delayed by the transfer. Starting an update while a cycle is already active is refused up front. |
| M79 | Flash a deliberately broken build (for example one that panics in `setup()` or cannot start HTTP) on the bench. | The controller boots it once, fails to serve the Web UI, and the bootloader returns to the previous slot. Recovery needs no USB. The diagnostic log of the recovered firmware shows `OTA rollback armed` or the panic reboot. |
| M80 | Immediately after an OTA restart, and before the new image is confirmed, attempt a second update. | It is refused with `PENDING_VERIFY`; after boot confirmation completes, the update is accepted normally; no browser request is required. |
| M81 | After flashing the native NimBLE production build (external host allocator; Web UI/Wi-Fi buffers in PSRAM; controller and task stacks internal), capture `/api/v1/status/diagnostic` at boot, host-ready, scanning, scale-connected and Companion-connected. Browse Settings/Presets/Admin for 10+ min with active polling, run a Wi-Fi scan, save settings once, and complete one OTA upload. Compare internal/PSRAM free, minimum and largest block, `bleRuntimeHostStackMinWords` (bytes), pool failures and `loopIntervalGapMs`. Confirm paddle and machine circuit still respond immediately. | Internal heap stays above health thresholds (48 KiB free / 16 KiB largest alert); memory reaches a plateau without stair-step loss; no pool exhaustion, `HEALTH_HEAP_LOW`, `NO_MEMORY`, cache-disabled panic or host reset; host stack retains at least 1536 bytes; settings, OTA and Wi-Fi scan succeed; machine circuit remains fail-open on errors. |
| M-AUD-04 | Isolated bench, machine circuit disconnected: start *Flash and restart* after a verified upload, then cut controller power during or immediately after the boot-partition switch (`esp_ota_set_boot_partition`). | After power returns the device boots either the new image as `PENDING_VERIFY` or the previous image. Machine circuit stays open. The controller is not bricked (USB not required if a previous slot exists). |
| M-AUD-05 | Isolated bench: complete commit (flash accepted) then cut power **before** the MCU restarts. | The next power-on boots the newly selected image as `PENDING_VERIFY`. Original slot remains the rollback target until confirm or the 180 s deadline. Machine circuit stays open. |
| M-AUD-06 | Isolated bench: flash an image that can run control (machine circuit stays open) but **never** starts HTTP. Repeat with a bootable previous slot, then on a board whose other app slot is not bootable. | With a previous slot: within ~180 s the log shows `OTA rollback armed` (or equivalent) and restart returns to the previous firmware; no USB. With no bootable previous slot: log shows `OTA rollback failed` / keeping the running image; the image is confirmed so the device still boots. Recover that case over USB. |

## Momentary behavior and scale discovery

| ID | Procedure | Expected result |
| --- | --- | --- |
| M82 | Switch-only (`SHOT_STOPPER_MACHINE_TYPE=1`), **Assume idle when the scale connects** ON: inferred brewing, no active shot, then connect a scale. Repeat with a brew cycle active. Repeat with the setting OFF. | Idle (no cycle): Home shows Confirmed off / Idle and the relay does not pulse. Active cycle: inferred state is unchanged and the shot continues. Setting OFF: connect does not settle idle. Copy never says the espresso machine powers off. |
| M83 | Switch-only: Start with a quiet pan until the shot-reaction timeout (default 12 s). Then let espresso-like flow begin. Change the timeout to 3 s and repeat the quiet nack. | After timeout, Home shows Assumed off (not Confirmed off / Idle) and the relay does not pulse. Late flow goes to Confirmed on with no extra pulse. The 3 s setting nacks sooner. |
| M84 | Switch-only: pull a BBW shot to target; wait the drip delay for a quiet, plausible settled weight. Repeat with Fast extraction guard (and with a shot shorter than 10 s so history/drip-delay finalize is skipped). Then stop a shot some other way (Stop / wall) and wait drip delay. | Target or extraction-guard cut: after a quiet pan (or drip delay when it runs), inferred state is Confirmed off. Home shows Assumed off while settling, not Assumed on. A short shot still settles on quiet without drip-delay finalize. User Stop / wall: quiet pan still settles to Confirmed off; drip delay is not required. No extra stop pulse after the cut pulse. |
| M85 | Switch-only Home, **without** Admin unlock (Web UI claimed): confirm Actions is visible. Tap **Override idle** while the group is electrically ON with no coffee; tap **Override brewing** while idle. Repeat on a reed or paddle build. | Switch-only: Override idle / Override brewing stay enabled except when another client owns the Web UI; they change inferred Idle / Confirmed on and do not pulse. Start/Stop/Rinse still need Admin. Reed/paddle: Override controls are hidden; `POST /api/v1/control/state-override` returns 409 `MACHINE_TYPE_UNSUPPORTED`. |
| M86 | Switch-only: assumed STOP (second press) while espresso-like flow continues. Then a firmware BBW cut while Confirmed on. | Assumed STOP + flow → Confirmed on, no extra pulse. Firmware cut still pulses (and retries if flow continues). |
| M87 | Momentary or reed, **Quick rinse** ON (off by default on every machine type): from idle, hold the switch for the rinse gesture (default 1 s). Repeat in Button release. Repeat a long-press during an already-running shot. Repeat with Quick rinse OFF. Then Armed no-scale BBW + idle long-press. | Idle hold: `RINSE`, start pulse (auto-stop pulse width), group stays on for rinse duration, then stop pulse. Release mode never starts a shot first. A long-press during assumed/confirmed on does not rinse. Quick rinse OFF: native 1:1, no `RINSE`. Armed idle long-press rinses, consumes the guard to Idle, and plays start + completion cues. |
| M88 | Bookoo Themis, machine circuit disconnected: power-cycle the scale once and wait for a stable GATT link. Confirm `Reconnects` increments by **1** (not +2) and `hciRxDropped=0` / `hciTxDropped=0` on `HEALTH` and diagnostic JSON. Pull a shot with the Web UI open; stream stays `FRESH`. Leave idle ~10 min (Companion factory-default OFF: the phone must not see `shotStopper` / `0xFFE`). Power the scale; discovery should complete in under ~2 s with default **Aggressive** scan intensity (100%: 20/20 ms). With Companion ON for next boot, idle advertise is slow (~320 ms) and does not drop the weight stream. Confirm a Themis advertisement that carries `ff11`/`ff12` (nRF Connect / sniffer) still connects if the GAP name is only in SCAN_RSP. After failed links, verify the bounded 50/80/100 ms backoff plus jitter cannot be bypassed by repeated advertisements and, with the 120 ms settle, never exceeds 250 ms of deliberate wait. Companion advertising should pause ~3 s after GAP (re)start, then resume while scanning. | One stable connect after power-cycle; no double reconnect burst; weight stays `FRESH` with the UI open; factory Companion OFF is invisible; Companion ON does not kill Bookoo notifications. Use **Normal** (50%: 31.25/62.5 ms) or **Light** (25%: 28.75/115) only if idle scanning needs less radio time. |

## Historical cutover evidence

M89–M93 preserve the accepted native-NimBLE cutover evidence. The internal
allocator and prior ArduinoBLE comparison images are historical, not current
production build selectors. Do not treat those past results as qualification
of later changes.

| ID | Procedure | Expected result |
| --- | --- | --- |
| M89 | On the same board and revision, flash the external and internal NimBLE allocator variants in turn. Repeat host-ready, Aggressive scanning, scale connected, Companion connected, Web UI polling, Wi-Fi scan and one OTA. Capture `HEALTH` and diagnostic snapshots after warm-up and at each state. | The selected allocator is reported correctly. Compare internal/PSRAM free, minimum and largest block, host stack high-water, CPU/radio and pool errors. There is no unaccounted PSRAM-to-internal fallback, mbuf exhaustion, reset or unsafe relay behavior. Keep external as candidate only if its full physical results pass. |
| M90 | With the chosen allocator, run scale stream plus STA or AP and Web UI polling continuously for at least 24 h. Capture metrics at fixed intervals and exercise 1,000 simulated cycles plus at least 100 physical scale power-off/on cycles. | After warm-up, free-heap drift is <=1 KiB and largest-block loss is <=2%, or an explicit bounded cache reaches a documented plateau. No panic, WDT, deadlock, phantom link, duplicated command, pool failure or unsafe close occurs. Host stack margin remains at least 1,536 bytes with its 4,096-byte stack. |
| M91 | Enable Companion for next boot and run at least 8 h while repeatedly connecting/disconnecting a supported phone client and alternating scale scan/connect/link. Include AP, STA, AP+STA, Web UI, Wi-Fi scan and RF-weak intervals. | All 17 characteristics, writes and notifications remain compatible; advertising pauses/resumes according to policy; scale data stays fresh; heap reaches a stable plateau and all fail-open behavior is preserved. |
| M92 | Perform 50 OTA/reboot cycles with the scale present or appearing during boot. Include native NimBLE upgrades, interrupted uploads and pending-verify rollback; confirm settings/NVS compatibility with the pre-cutover image used for release qualification. | Every valid image confirms or rolls back deterministically; no device is bricked; settings/NVS, Wi-Fi, presets, calibration and history survive; machine circuit stays open through every boot and failure. |
| M93 | From captured timestamps, calculate p50/p95/p99 for compatible advertisement→connect issued, advertisement→READY, disconnect→published disconnected and disconnect→new scan. Repeat the relevant matrix on n8r4 and n16r8 and record the bounded beta result with backend visible in diagnostic. | Aggressive advertisement→connect p95 <=100 ms; reference advertisement→READY p95 <=2 s; disconnect publication and new scan p95 <=100 ms unless an explicit RF policy delays it; no p99 regression, model compatibility loss or release-blocking beta regression. |
